<%= form_with(model: select_place, local: true) do |form| %>
  <% if select_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(select_place.errors.count, "error") %> prohibited this select_place from being saved:</h2>

      <ul>
      <% select_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id, id: :select_place_id %>
  </div>

  <div class="field">
    <%= form.label :club_id %>
    <%= form.number_field :club_id, id: :select_place_club_id %>
  </div>

  <div class="field">
    <%= form.label :place_id %>
    <%= form.number_field :place_id, id: :select_place_place_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
